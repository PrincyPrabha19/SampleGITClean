<UserControl x:Class="Dominator.UI.Views.MainOCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"              
             Background="{x:Null}"
             d:DesignHeight="483" d:DesignWidth="1030">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ColumnDefinition0" Width="300"/>
            <ColumnDefinition x:Name="ColumnDefinition1" Width="*"/>
        </Grid.ColumnDefinitions>

        <ContentControl Margin="30,20,20,20" Content="{Binding ProfileNavigationViewModel.View}" />
        <ContentControl Grid.Column="1" Margin="20" Content="{Binding ProfileDetailsViewModel.View}" />
    </Grid>
</UserControl>
